import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        ink: "#0F1A1F",
        brand: "#1A5C3A",
        canvas: "#F5F3EE",
        healthy: "#4A7A5A",
        monitor: "#C68A0E",
        risk: "#C0392B",
      },
    },
  },
};

export default config;
